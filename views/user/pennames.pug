p:b Pen Names
p These are names under which you have published works. Storing pen names helps #{appconf.customtext.appname} match a work to your profile and the profiles of other #{appconf.customtext.orgshortname} contributors.

ul.collection
  each pn in pennames
    li.collection-item= pn
    
p:b Add New Pen Name
div.row
  form.col.s12.m8.l6.offset-m2.offset-l3(method="post", action="/user/penname")
    div.input-field
      input#first_name.validate(name="first_name", type="text")
      label(for="first_name") Given/First Name
    div.input-field
      input#middle_name.validate(name="middle_name", type="text")
      label(for="middle_name") Middle Name
    div.input-field
      input#last_name.validate(name="last_name", type="text")
      label(for="last_name") Surname/Last Name
    div.input-field
      button.btn.blue.darken-4.waves-effect.waves-light(type="submit") Add

if success.length
  script Materialize.toast(#{success}, 3000, 'green darken-1 white-text');

if error.length
  script Materialize.toast(#{error}, 5000, 'red white-text');
