if success.length
  script M.toast({html:"#{success}", classes:'green darken-1 white-text'});

if error.length
  script M.toast({html:"#{error}", displayLength: 6000, classes:'red darken-4 white-text'});
